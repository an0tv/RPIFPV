# PiCam portal — web UI + config API on :8080
[Unit]
Description=PiCam web portal (stream view + wifi config)
After=network-online.target picam-net.service
Wants=network-online.target

[Service]
Type=simple
User=root
ExecStart=/usr/bin/python3 /opt/picam/web/portal.py
Restart=always
RestartSec=3
Environment=PORT=8080

[Install]
WantedBy=multi-user.target
